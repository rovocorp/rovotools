# Copy to .env for local development. Only EXPO_PUBLIC_* values ship in builds.
# Never put secrets here — there are no authenticated backends yet.
EXPO_PUBLIC_ADS_ENABLED=false
EXPO_PUBLIC_API_URL=https://rovotools.com
